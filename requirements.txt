# =============================================================================
# HVAC Market Analysis — Python Dependencies
# =============================================================================
# Installation : pip install -r requirements.txt
# For Deep Learning (optional) : pip install -r requirements-dl.txt
# For the API (optional) : pip install -r requirements-api.txt
# =============================================================================

# === Data Collection ===
requests>=2.31.0,<3.0.0         # HTTP requests (all APIs)
eurostat>=1.1.0,<2.0.0          # Dedicated Eurostat package

# === Data Processing ===
pandas>=2.1.0,<3.0.0            # DataFrames and data manipulation
numpy>=1.24.0,<2.0.0            # Numerical computing — bound <2.0 for Prophet compatibility
openpyxl>=3.1.0,<4.0.0          # Excel file read/write

# === Database ===
sqlalchemy>=2.0.0,<3.0.0        # ORM and SQL abstraction
# pyodbc>=5.1.0                  # SQL Server — uncomment if DB_TYPE=mssql
# psycopg2-binary>=2.9.0        # PostgreSQL — uncomment if DB_TYPE=postgresql

# === Analysis & Visualization ===
matplotlib>=3.7.0,<4.0.0        # Static charts
seaborn>=0.13.0,<1.0.0          # Statistical charts
plotly>=5.18.0,<6.0.0           # Interactive charts
scipy>=1.11.0,<2.0.0            # Scientific functions
statsmodels>=0.14.0,<1.0.0      # Statistical models and decomposition

# === Machine Learning ===
scikit-learn>=1.3.0,<2.0.0      # Classic ML (Ridge, RandomForest, pipelines)
xgboost>=2.0.0,<3.0.0           # Gradient boosting
lightgbm>=4.1.0,<5.0.0          # Gradient boosting (better on small datasets)
prophet>=1.1.5,<2.0.0           # Facebook time series forecasting
shap>=0.43.0,<1.0.0             # Interpretability (feature importance)

# === Web Interface ===
streamlit>=1.30.0,<2.0.0        # Interactive dashboard

# === Notebooks ===
jupyter>=1.0.0,<2.0.0           # Jupyter notebooks
ipykernel>=6.25.0,<7.0.0        # Python kernel for Jupyter

# === Utilities ===
python-dotenv>=1.0.0,<2.0.0     # Environment variable loading
tqdm>=4.66.0,<5.0.0             # Progress bars
lxml>=4.9.0,<6.0.0              # XML parsing (INSEE SDMX)
pyarrow>=14.0.0,<18.0.0         # Parquet format (optimized storage)
