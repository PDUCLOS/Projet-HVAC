# =============================================================================
# HVAC Market Analysis — API Dependencies
# =============================================================================
# Installation : pip install -r requirements-api.txt
# Also requires : pip install -r requirements.txt (for pandas, scikit-learn, etc.)
# Launch   : uvicorn api.main:app --reload
# =============================================================================

fastapi>=0.104.0,<1.0.0         # Async API framework
uvicorn[standard]>=0.24.0,<1.0.0  # ASGI server (with watchfiles for --reload)
pydantic>=2.5.0,<3.0.0          # Data validation and serialization
