# =============================================================================
# Configuration du projet HVAC Market Analysis
# =============================================================================
# Copier ce fichier en .env et adapter les valeurs si nécessaire
# Usage : les valeurs par défaut fonctionnent pour un setup local standard
# =============================================================================

# --- Base de données ---
# SQLite par défaut (aucune installation requise, fichier local)
DB_TYPE=sqlite
DB_PATH=data/hvac_market.db

# SQL Server (décommenter pour basculer)
# Prérequis : pip install pyodbc
# DB_TYPE=mssql
# DB_HOST=localhost
# DB_PORT=1433
# DB_NAME=hvac_market
# DB_USER=                          # Laisser vide = Windows Authentication
# DB_PASSWORD=                      # Laisser vide = Windows Authentication
# DB_DRIVER=ODBC Driver 17 for SQL Server
# ALLOW_NON_LOCAL=true              # Obligatoire pour bases non-SQLite

# PostgreSQL (décommenter pour basculer)
# Prérequis : pip install psycopg2-binary
# DB_TYPE=postgresql
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=hvac_market
# DB_USER=patrice
# DB_PASSWORD=your_password
# ALLOW_NON_LOCAL=true

# --- Période de collecte ---
DATA_START_DATE=2019-01-01
DATA_END_DATE=2026-02-28

# --- Perimetre geographique ---
# FR = France metropolitaine entiere (96 departements, defaut)
# 84 = Auvergne-Rhone-Alpes uniquement (12 departements)
# 11 = Ile-de-France, 93 = PACA, etc.
TARGET_REGION=FR

# Override manuel : liste de departements specifiques (prioritaire sur TARGET_REGION)
# TARGET_DEPARTMENTS=01,07,26,38,42,69,73,74

# --- Répertoires de données ---
RAW_DATA_DIR=data/raw
PROCESSED_DATA_DIR=data/processed
FEATURES_DATA_DIR=data/features

# --- APIs (aucune clé requise pour les sources utilisées) ---
OPENMETEO_BASE_URL=https://archive-api.open-meteo.com/v1/archive
INSEE_BDM_URL=https://www.bdm.insee.fr/series/sdmx/data/SERIES_BDM

# --- Paramètres réseau ---
REQUEST_TIMEOUT=30
MAX_RETRIES=3
RETRY_BACKOFF=1.0
RATE_LIMIT_DELAY=0.5

# --- pCloud (synchronisation automatique des données) ---
# Code du lien public pCloud (extrait de l'URL de partage)
PCLOUD_PUBLIC_CODE=kZbQQ3Zg1slD5WfRgh42fH5rRpDDYWyBEsy
# Token d'accès OAuth2 (optionnel, pour les dossiers privés)
# PCLOUD_ACCESS_TOKEN=

# --- Logging ---
LOG_LEVEL=INFO
