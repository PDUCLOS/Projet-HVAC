# =============================================================================
# HVAC Market Analysis Project Configuration
# =============================================================================
# Copy this file to .env and adjust values if needed
# Usage : default values work for a standard local setup
# =============================================================================

# --- Database ---
# SQLite by default (no installation required, local file)
DB_TYPE=sqlite
DB_PATH=data/hvac_market.db

# SQL Server (uncomment to switch)
# Prerequisite : pip install pyodbc
# DB_TYPE=mssql
# DB_HOST=localhost
# DB_PORT=1433
# DB_NAME=hvac_market
# DB_USER=                          # Leave empty = Windows Authentication
# DB_PASSWORD=                      # Leave empty = Windows Authentication
# DB_DRIVER=ODBC Driver 17 for SQL Server
# ALLOW_NON_LOCAL=true              # Required for non-SQLite databases

# PostgreSQL (uncomment to switch)
# Prerequisite : pip install psycopg2-binary
# DB_TYPE=postgresql
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=hvac_market
# DB_USER=patrice
# DB_PASSWORD=your_password
# ALLOW_NON_LOCAL=true

# --- Data collection period ---
DATA_START_DATE=2019-01-01
DATA_END_DATE=2026-02-28

# --- Geographic scope ---
# FR = Entire metropolitan France (96 departments, default)
# FR = Metropolitan France (96 departments)
# 11 = Ile-de-France, 93 = PACA, etc.
TARGET_REGION=FR

# Manual override : specific department list (takes priority over TARGET_REGION)
# TARGET_DEPARTMENTS=01,07,26,38,42,69,73,74

# --- Data directories ---
RAW_DATA_DIR=data/raw
PROCESSED_DATA_DIR=data/processed
FEATURES_DATA_DIR=data/features

# --- APIs (no key required for the sources used) ---
OPENMETEO_BASE_URL=https://archive-api.open-meteo.com/v1/archive
INSEE_BDM_URL=https://www.bdm.insee.fr/series/sdmx/data/SERIES_BDM

# --- Network settings ---
REQUEST_TIMEOUT=30
MAX_RETRIES=3
RETRY_BACKOFF=1.0
RATE_LIMIT_DELAY=0.5

# --- pCloud (automatic data synchronization) ---
# Public link code from pCloud (extracted from the sharing URL)
PCLOUD_PUBLIC_CODE=kZbQQ3Zg1slD5WfRgh42fH5rRpDDYWyBEsy
# OAuth2 access token (optional, for private folders)
# PCLOUD_ACCESS_TOKEN=

# --- Logging ---
LOG_LEVEL=INFO
